<div class="max-w-lg mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
  <h1 class="text-2xl font-bold mb-4">User Profile</h1>
  
  <p><strong>Username:</strong> <%= @user.name %></p>
  <p><strong>Email:</strong> <%= @user.email_address %></p>
  <p><strong>Date Joined:</strong> <%= @user.created_at.strftime("%B %d, %Y") %></p>

  <div class="mt-6 p-4 bg-blue-100 rounded-lg">
    <h2 class="text-lg font-semibold">A Positive Message for You:</h2>
    <p class="italic">"Keep pushing forward! Your journey is just beginning."</p>
  </div>

  <%= link_to "Edit Profile", edit_user_path(@user), class: "mt-4 inline-block text-white bg-green-500 px-4 py-2 rounded hover:bg-green-600" %>
</div>
