<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Dashboard</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .container { max-width: 1200px; margin: auto; padding: 20px; }
        .nav { display: flex; justify-content: space-between; padding: 10px; background: #4CAF50; color: white; }
        .books-section { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; }
        .book-card { padding: 10px; border: 1px solid #ddd; border-radius: 5px; background: #f9f9f9; }
    </style>
</head>
<body>
    <div class="nav">
        <div>
            <a href="/" style="color: blue; text-decoration: none;">Home</a>
            <a href="/books" style="color: blue; text-decoration: none; margin-left: 20px;">Book Borrowing</a>
            <a href="/read_books" style="color: blue; text-decoration: none; margin-left: 20px;">Read Books</a>
            <a href="/profile" style="color: blue; text-decoration: none; margin-left: 20px;">User Profile</a>
        </div>
        <div>
            <a href="/logout" style="color: blue; text-decoration: none;">Logout</a>
        </div>
    </div>
    
    <div class="container">
    <h2>Welcome, <%= current_user.username %>!</h2>
    
    <h3>Borrowed Books</h3>
    <div class="books-section">
    <% if @borrowed_books.present? %>
    <% @borrowed_books.each do |book| %>
        <div class="book-card"><%= book.title %> - Borrowed</div>
    <% end %>
    <% else %>
    <p>No borrowed books yet.</p>
    <% end %>
    </div>
    
    <h3>Returned Books</h3>
    <div class="books-section">
        <% @returned_books.each do |book| %>
            <div class="book-card"><%= book.title %> - Returned</div>
        <% end %>
    </div>
    
    <h3>Relevant Books for You</h3>
    <div class="books-section">
        <% @relevant_books.each do |book| %>
            <div class="book-card"><%= book.title %> - Available</div>
        <% end %>
    </div>
    </div>

</body>
</html>
